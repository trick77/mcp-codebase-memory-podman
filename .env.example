# Build-time config for codebase-memory-mcp. Copy to .env and edit.
# .env is gitignored.

# npm registry override (e.g. corporate mirror).
#NPM_REGISTRY=https://nexus.example.com/repository/npm-all

# Pin a specific upstream release tag. Leave unset for the latest GitHub
# release. Use the leading 'v' (that's the upstream git tag).
#VERSION=v0.4.11
